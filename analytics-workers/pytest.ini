# Module: analytics-worker pytest configuration.
# Role: registers opt-in integration tiers while retaining normal discovery.
# Dependencies: pytest reads this file when invoked from analytics-workers.
# Side effects: marker expressions can include or exclude hardware tests.

[pytest]
markers =
    gpu: requires an explicitly enabled NVIDIA CUDA fine-tuning environment
