# Module: browser-visible CoreMesh gateway configuration.
# Role: selects the single API origin used by frontend-ui.
# Dependencies: Vite exposes variables prefixed with VITE_ at build time.
# Side effects: this URL receives all browser API traffic.
VITE_GATEWAY_BASE_URL=http://localhost:8080
